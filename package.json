{
  "name": "ai-pokemon-teambuilder-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --check app.js && node --check team-client.js",
    "test": "node --test"
  }
}
